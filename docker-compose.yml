version: '3.8'

services:
  node-app:
    image: node:22
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - ./.env
    network_mode: host
    command: sh -c "npm install && npm run build && npm start"
